problem id: 2f6531cb
category: cryptarithm_deduce

symbol-to-digit mapping:

operator-to-operation mapping:
  '\\' = concat

examples:
  (&>{@ = >^@
  ({}$' = @:(
  '&\"& = '&"&

query: @:\:'

predicted answer: @::'
