Zero shot predictions
Repository note: these zero-shot observations are exploratory baseline notes, but they do not replace the repository competition contract. In this repo, the authoritative evaluation and submission contract remains the top-level `README.md` (`max_lora_rank = 32`, `max_tokens = 7680`, `top_p = 1.0`, `temperature = 0.0`, `max_num_seqs = 64`, `gpu_memory_utilization = 0.85`, `max_model_len = 8192`, final artifact `submission.zip`). Treat the findings below as reference observations rather than as authoritative evaluation behavior.

If ur intereseted i created notebook, where I test models zero shot capability.

notebook:discussion/train-set-zero-shot-inference-exploration.ipynb

Interesting findings:

number conversion 100% accuracy
bit,equaton and ecryption it does horribly
gravitation and mesurement it has like 33% success
Also notebook contains csv with model predictions as raw text if u want to analyze it more.
